SSD1309 全亮指令完整流程（直接复制执行，100% 点亮）
全亮流程 = 「初始化寄存器」+「设置显示范围」+「发送全亮数据」，所有步骤按顺序通过 I2C 发送，地址默认用最常用的 0x78（若你确认地址是 0x7A，替换即可）。
一、核心规则（必看）
I2C 发送格式：START → 从机地址（写）→ 控制字节（0x00=发命令，0x40=发数据）→ 指令/数据 → ACK → ... → STOP
所有指令按顺序发送，无需额外等待，执行完最后一步屏幕直接全亮。
二、完整指令序列（按顺序复制发送）
第一步：初始化核心寄存器（确保屏幕能正常工作）
plaintext
// 1. 关闭显示（初始化前先关，避免乱码）
START → 0x78 → 0x00 → 0xAE → ACK
// 2. 设置显示时钟分频比/振荡器频率（默认配置，稳定显示）
START → 0x78 → 0x00 → 0xD5 → ACK → 0x80 → ACK
// 3. 设置多路复用率（128x64屏固定为0x3F，对应64行）
START → 0x78 → 0x00 → 0xA8 → ACK → 0x3F → ACK
// 4. 设置显示偏移（无偏移，默认0x00）
START → 0x78 → 0x00 → 0xD3 → ACK → 0x00 → ACK
// 5. 设置显示起始行（从第0行开始，默认0x40）
START → 0x78 → 0x00 → 0x40 → ACK
// 6. 开启电荷泵（关键！不开启屏幕不亮，参数0x14=开启）
START → 0x78 → 0x00 → 0x8D → ACK → 0x14 → ACK
// 7. 设置内存寻址模式（水平寻址，方便连续发数据）
START → 0x78 → 0x00 → 0x20 → ACK → 0x00 → ACK
// 8. 设置段重映射（左右正常显示，0xA1=反向，0xA0=正向，按需选择）
START → 0x78 → 0x00 → 0xA1 → ACK
// 9. 设置COM输出扫描方向（上下正常显示，0xC8=反向，0xC0=正向，按需选择）
START → 0x78 → 0x00 → 0xC8 → ACK
// 10. 设置COM引脚硬件配置（128x64屏固定0x12）
START → 0x78 → 0x00 → 0xDA → ACK → 0x12 → ACK
// 11. 设置对比度（默认0xCF，亮度适中，可调整0x00~0xFF）
START → 0x78 → 0x00 → 0x81 → ACK → 0xCF → ACK
// 12. 设置预充电周期（默认配置0xF1）
START → 0x78 → 0x00 → 0xD9 → ACK → 0xF1 → ACK
// 13. 设置VCOMH deselected电平（默认0x30）
START → 0x78 → 0x00 → 0xDB → ACK → 0x30 → ACK
// 14. 恢复RAM内容显示（不强制全亮，按数据显示）
START → 0x78 → 0x00 → 0xA4 → ACK
// 15. 正常显示模式（非反色，0xA6=正常，0xA7=反色）
START → 0x78 → 0x00 → 0xA6 → ACK
第二步：设置显示范围（全屏幕覆盖：列 0~127，页 0~7）
plaintext
// 1. 设置列地址范围（0x21=列地址命令，参数0x00=起始列，0x7F=结束列）
START → 0x78 → 0x00 → 0x21 → ACK → 0x00 → ACK → 0x7F → ACK
// 2. 设置页地址范围（0x22=页地址命令，参数0x00=起始页，0x07=结束页，共8页）
START → 0x78 → 0x00 → 0x22 → ACK → 0x00 → ACK → 0x07 → ACK
第三步：发送全亮数据（1024 字节 0xFF，填满整个屏幕 RAM）
屏幕分辨率 128x64，共 8 页（每页 8 行），每页需发送 128 字节数据，8 页 ×128 字节 = 1024 字节，全部发送 0xFF（表示像素点亮）：
plaintext
START → 0x78 → 0x40 → 0xFF → ACK → 0xFF → ACK → ...（共发送1024次0xFF）→ STOP
简化操作：I2C 工具支持 “连续发送相同数据”，直接设置 “发送 1024 字节，数据值 0xFF” 即可，无需手动重复。
第四步：开启显示（最后一步，点亮屏幕）
plaintext
START → 0x78 → 0x00 → 0xAF → ACK → STOP
三、关键说明
若地址是 0x7A：将所有序列中的 0x78 替换为 0x7A，其他不变；
全亮原理：0xFF 表示每个像素点 “点亮”，1024 字节刚好填满 128x64 的 RAM，因此整个屏幕全亮；
取消全亮：将第三步的 0xFF 改为 0x00（像素熄灭），重新发送即可全灭；
亮度调整：修改 “设置对比度” 步骤的参数（0x81 后的 0xCF），范围 0x00（最暗）~0xFF（最亮）。

还有从设备的地址 我的是0X78,写命令是从0X00开始，写数据是从0x40开始(内部芯片是SSD1309) 

步骤 3：发送初始化命令序列（全程命令 = 控制字节 0x00）
每一条命令都按：
START → 0x78 → ACK → 0x00 → ACK → 命令 → ACK → STOP
按顺序发：
0xAE （关闭显示）
0xD5 0x80 （时钟分频）
0xA8 0x3F （多路复用 64）
0xD3 0x00 （显示偏移）
0x40 （起始行）
0x8D 0x14 （电荷泵使能）
0x20 0x02 （页地址模式）
0xA1 （段重映射）
0xC8 （COM 扫描方向）
0xDA 0x12 （COM 引脚配置）
0x81 0xCF （对比度）
0xD9 0xF1 （预充电周期）
0xDB 0x30 （VCOMH）
0xA4 （显示遵循 RAM）
0xA6 （正常显示，不反色）
0xAF （打开显示）
步骤 4：设置页地址 + 列地址（命令）
依然是命令，控制字节 0x00：
0xB0 + 页号（0~7） → 选第 0 页就是 0xB0
0x00 → 列地址低 4 位
0x10 → 列地址高 4 位
步骤 5：发送全屏数据 0xFF（真正全亮）
发数据时：控制字节 = 0x40
每一页都要：
START → 0x78 → ACK → 0x40 → ACK → 连续发128个 0xFF → ACK → STOP
完整数据发送流程：
循环 8 次（8 页）：
发命令设置当前页（0xB0 ~ 0xB7）
发命令设置列起始（0x00、0x10）
发 IIC START
发 0x78 等待 ACK
发 0x40（数据标志）等待 ACK
连续发送 128 个 0xFF（每个都等 ACK）
发 IIC STOP
8 页全部发完，屏幕100% 全亮。

一、先统一 IIC 发送格式（必须懂）
所有命令 / 数据都按这个格式发：
发命令：START → 0x78 → ACK → 0x00 → ACK → 命令 → ACK → STOP
发数据：START → 0x78 → ACK → 0x40 → ACK → 数据 → ACK → STOP
二、SSD1309 全屏全亮 完整命令 + 作用 + 可设参数
1）0xAE —— 关闭显示（DISPLAY OFF）
作用：先关屏，防止初始化过程乱闪
可设置参数：
0xAE = 关闭
0xAF = 打开
2）0xD5 0x__ —— 设置显示时钟 DIVIDE & OSCILLATOR
作用：设置屏幕刷新时钟频率、分频系数
命令格式：0xD5 + [分频 + 频率]
可设置参数（一个字节）：
高 4 位：震荡频率 Fosc（0~15），越大刷新越快
低 4 位：分频比（1~16）
常用值：0x80（默认，最稳）
3）0xA8 0x3F —— 设置多路复用率 MULTIPLEX RATIO
作用：设置 COM 数量，128×64 屏必须设 63（64 路）
命令格式：0xA8 + [比值 - 1]
可设置参数：
0x0F ~ 0x3F
64 行屏 → 必须写 0x3F（64-1）
4）0xD3 0x00 —— 设置显示偏移 DISPLAY OFFSET
作用：让整体画面上下挪几行
命令格式：0xD3 + [偏移行数]
可设置参数：0x00 ~ 0x3F
0x00 = 不偏移
数值越大，画面越往下移
5）0x40 —— 设置显示起始行 START LINE
作用：设置从第几行开始显示
命令格式：0x40 + 行号（0~63）
可设置参数：0x40 ~ 0x7F
0x40 = 从第 0 行开始
0x41 = 从第 1 行开始
6）0x8D 0x14 —— 电荷泵使能 CHARGE PUMP
作用：OLED 必须开电荷泵，否则不亮
命令格式：0x8D + 使能值
可设置参数：
0x10 = 关闭
0x14 = 打开（必须）
7）0x20 0x02 —— 设置内存地址模式 MEMORY ADDRESSING MODE
作用：决定你怎么写屏（页 / 水平 / 垂直）
命令格式：0x20 + 模式
可设置参数：
0x00 = 水平模式
0x01 = 垂直模式
0x02 = 页模式（最常用）
8）0xA1 —— 段重映射 SEGMENT REMAP
作用：左右翻转屏幕
可设置参数：
0xA0 = 正常方向
0xA1 = 左右翻转（常用）
9）0xC8 —— COM 扫描方向 COM SCAN DIRECTION
作用：上下翻转屏幕
可设置参数：
0xC0 = 正常方向
0xC8 = 上下翻转（常用）
10）0xDA 0x12 —— 设置 COM 引脚配置 COM PINS
作用：匹配硬件 COM 引脚排列
命令格式：0xDA + 配置值
可设置参数：
0x02 = 顺序模式
0x12 = 交替模式（128×64 必用）
0x22 = 其他配置
11）0x81 0xCF —— 设置对比度 CONTRAST
作用：调亮度
命令格式：0x81 + 对比度值
可设置参数：0x00 ~ 0xFF
0x00 = 最暗
0xCF = 常用
0xFF = 最亮
12）0xD9 0xF1 —— 设置预充电周期 PRECHARGE PERIOD
作用：控制 OLED 像素充放电时间，影响亮度与寿命
命令格式：0xD9 + [周期]
可设置参数：
高 4 位：预充电周期
低 4 位：放电周期
常用：0xF1、0x22
13）0xDB 0x30 —— 设置 VCOMH 取消选择电平
作用：设置 COM 引脚电压
命令格式：0xDB + 电平值
可设置参数：
0x00、0x20、0x30（最常用）
14）0xA4 —— 全局显示遵循 RAM
作用：让屏幕显示你写的 GRAM 内容
可设置参数：
0xA4 = 正常显示 RAM 内容
0xA5 = 全屏强制点亮（无视 RAM）
15）0xA6 —— 正常显示 / 反色显示
作用：黑白反转
可设置参数：
0xA6 = 正常（0 灭 1 亮）
0xA7 = 反色（0 亮 1 灭）
16）0xAF —— 打开显示 DISPLAY ON
作用：初始化完成，开屏
可设置参数：
0xAE = 关
0xAF = 开
三、地址设置命令（写全屏必须用）
1）0xB0~0xB7 —— 设置页地址（PAGE）
作用：选择当前写哪一页（共 8 页）
可设置参数：
0xB0 = 第 0 页
0xB1 = 第 1 页
...
0xB7 = 第 7 页
2）0x00~0x0F —— 列地址低 4 位
3）0x10~0x1F —— 列地址高 4 位
组合起来定位到某一列
通常用：0x00 + 0x10 = 从第 0 列开始
四、写全屏数据命令（真正全亮）
命令：无，直接写数据
数据：0xFF（每个字节 8 个像素全亮）
发送格式：
START → 0x78 → 0x40 → 连续 128 个 0xFF → STOP
循环 8 页，屏幕100% 全亮
五、你要的「完整可直接用」总结
复位 RES
发 16 条初始化命令
循环 8 页：
发页地址 + 列地址
发 128 个 0xFF
屏幕全亮

命令序列	功能说明	可设置参数	完整发送流程（IIC）
0xAE	关闭显示（初始化先关屏）	0xAE = 关；0xAF = 开	START→0x78→0x00→0xAE→STOP
0xD5 0x80	设置时钟分频 + 震荡频率	高 4 位：频率 (0~15)；低 4 位：分频 (1~16)；默认 0x80	START→0x78→0x00→0xD5→0x80→STOP
0xA8 0x3F	设置多路复用率（64 行屏）	0x0F~0x3F；64 行固定 0x3F	START→0x78→0x00→0xA8→0x3F→STOP
0xD3 0x00	设置显示偏移（上下移位）	0x00~0x3F；0x00 = 不偏移	START→0x78→0x00→0xD3→0x00→STOP
0x40	设置显示起始行	0x40~0x7F；0x40 = 第 0 行开始	START→0x78→0x00→0x40→STOP
0x8D 0x14	电荷泵使能（OLED 必开）	0x10 = 关；0x14 = 开	START→0x78→0x00→0x8D→0x14→STOP
0x20 0x02	设置内存地址模式	0x00 = 水平；0x01 = 垂直；0x02 = 页模式 (常用)	START→0x78→0x00→0x20→0x02→STOP
0xA1	段重映射（左右翻转）	0xA0 = 正常；0xA1 = 翻转	START→0x78→0x00→0xA1→STOP
0xC8	COM 扫描方向（上下翻转）	0xC0 = 正常；0xC8 = 翻转	START→0x78→0x00→0xC8→STOP
0xDA 0x12	COM 引脚硬件配置	0x02/0x12/0x22；128×64 固定 0x12	START→0x78→0x00→0xDA→0x12→STOP
0x81 0xCF	对比度（亮度调节）	0x00 (最暗)~0xFF (最亮)；默认 0xCF	START→0x78→0x00→0x81→0xCF→STOP
0xD9 0xF1	预充电周期	高 4 位充电 / 低 4 位放电；常用 0xF1/0x22	START→0x78→0x00→0xD9→0xF1→STOP
0xDB 0x30	VCOMH 取消选择电平	0x00/0x20/0x30；常用 0x30	START→0x78→0x00→0xDB→0x30→STOP
0xA4	显示遵循 RAM 内容	0xA4 = 正常；0xA5 = 全屏强制亮	START→0x78→0x00→0xA4→STOP
0xA6	正常 / 反色显示	0xA6 = 正常；0xA7 = 反色	START→0x78→0x00→0xA6→STOP
0xAF	打开显示（初始化完成）	0xAE = 关；0xAF = 开	START→0x78→0x00→0xAF→STOP

命令序列	功能说明	可设置参数	完整发送流程
0xB0~0xB7	设置页地址（共 8 页）	0xB0 = 页 0；0xB1 = 页 1…0xB7 = 页 7	START→0x78→0x00→0xB0→STOP
0x00~0x0F	列地址低 4 位	0x00 = 列起始低地址	START→0x78→0x00→0x00→STOP
0x10~0x1F	列地址高 4 位	0x10 = 列起始高地址	START→0x78→0x00→0x10→STOP

三、全屏全亮发送流程（直接用）
硬件复位：RES 拉低 10ms→拉高 10ms
发送全部初始化命令
循环 8 页（页 0~ 页 7）：
发页地址：START→0x78→0x00→(0xB0 + 页号)→STOP
发列地址：START→0x78→0x00→0x00→STOP
发列地址：START→0x78→0x00→0x10→STOP
发全屏数据：START→0x78→0x40→128 个 0xFF→STOP
屏幕全亮完成
四、常用快捷命令
关屏：0xAE
开屏：0xAF
反色显示：0xA7
正常显示：0xA6
全屏强制点亮：0xA5
亮度最亮：0x81 0xFF